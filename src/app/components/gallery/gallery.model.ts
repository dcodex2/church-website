export interface GalleryEvent {
  id: string;
  name: string;
  date: string;
  coverImage: string;
}

export interface GalleryImage {
  id: string;
  imageUrl: string;
  title: string;
  description: string;
}
